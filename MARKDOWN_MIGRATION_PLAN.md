# Markdown ドキュメント移行計画

## 1. 目的

`docs/` 内のMarkdownを、既存のcompetitive-verifierおよびJekyllによる公開方式を維持したまま、一定の形式で効率的に管理できる状態へ移行する。

この移行では、次の状態を目標とする。

- 新規文書を共通テンプレートから作成できる
- 保存時またはコマンド実行時に安全な範囲を自動整形できる
- フォーマットの崩れや必須項目の不足をCIで検出できる
- 既存文書の内容や数式、コード例を意図せず変更しない
- `documentation_of`と実装ファイルの対応を維持する

## 2. 現状

調査時点の`docs/`には151個、約8,100行のMarkdownがある。

| 項目                                     |    現状 |
| ---------------------------------------- | ------: |
| `documentation_of`がある文書             |     151 |
| `documentation_of`の参照先が存在する文書 |     151 |
| `title`がない文書                        |      61 |
| 最初の見出しがH1の文書                   |      70 |
| 最初の見出しがH2の文書                   |      80 |
| 本文見出しがない文書                     |       1 |
| `*`形式のリスト項目                      |   345行 |
| `-`形式のリスト項目                      |   651行 |
| 言語指定のないコードブロック             | 約100個 |
| `cpp`指定のコードブロック                | 約293個 |
| 行末空白                                 |    46行 |

文書は、主に次の2形式に分かれている。

1. `## 概要`と箇条書きを中心とした旧形式
2. API名をH1、制約や計算量をH2にした新形式

## 3. 移行後の標準形式

各文書は原則として次の形式に統一する。

````markdown
---
title: Binary Indexed Tree
documentation_of: //structure/others/binary-indexed-tree.hpp
---

概要を日本語で記述する。

# コンストラクタ

```cpp
BinaryIndexedTree(int n)
```

説明を記述する。

## 制約

- 制約を記述する

## 計算量

- $O(n)$

# apply

```cpp
void apply(int k, const T &x)
```

説明を記述する。
````

表記ルールは次のとおりとする。

- front matterには`title`と`documentation_of`を必須とする
- `title`内の英単語は空白で区切り、日本語の補足を丸括弧で併記する場合は丸括弧の前に空白を1つ入れる
- API、関数、コンストラクタの見出しにはH1を使う
- API内の`制約`、`計算量`、補足説明にはH2を使う
- 箇条書きには`-`を使う
- C++コードブロックには`cpp`を指定する
- 日本語本文には原則として`、`と`。`を使う
- 意味のあるMarkdownの強制改行を除き、行末空白を入れない
- 数式や識別子の表記は、機械的に変更しない

ページタイトルはfront matterから生成されるため、本文に文書タイトル用のH1は追加しない。

## 4. 移行方針

移行は小さな差分に分割する。フォーマット変更と内容修正を同じ変更に混在させない。

### フェーズ1：基準と検査方法の確定

実施内容：

- 本文書の標準形式をレビューして確定する
- 新規文書用テンプレートを追加する
- markdownlintの設定を追加する
- Prettierの設定を追加する
- ローカルでlintと整形確認を実行できるコマンドを用意する
- CIへは、最初は結果確認のみ、または既存文書を失敗させない形で組み込む

注意事項：

- front matterの`title`をページタイトルとして使うため、本文H1を必須とするルールは無効化または調整する
- 日本語本文や数式の意図しない折り返しを防ぐ設定を選ぶ
- Markdown内のHTMLやJekyll記法が存在する場合に壊れないことを確認する

完了条件：

- 標準テンプレートから新しい文書を作成できる
- ローカルとCIで同じ検査結果になる
- 現在のcompetitive-verifier/Jekyllビルドが成功する

### フェーズ2：意味を変えない機械的修正

次の変更だけをまとめて適用する。

- 行末空白の削除
- 箇条書きの`*`から`-`への変更
- C++コードブロックへの`cpp`指定
- front matter周辺の空行調整

次の項目は、このフェーズでは変更しない。

- 見出しレベル
- 日本語の文章
- 数式
- API名やシグネチャ
- 計算量や制約の内容

完了条件：

- 対象差分が機械的変更だけである
- コードブロックの開始と終了がすべて対応している
- competitive-verifier/Jekyllビルドが成功する
- 公開ページの代表例を目視確認できる

### フェーズ3：front matterの統一

`title`がない61文書へ、実装名や既存ページ表示に基づいた`title`を追加する。

手順：

1. ファイル名、対象hpp、既存ページ表示からタイトル候補を生成する
2. 略称、大文字小文字、スペルを確認する
3. 小さな単位に分けて追加する
4. URLやページ一覧の表示が変わっていないことを確認する

完了条件：

- すべての文書に`title`と`documentation_of`がある
- すべての`documentation_of`参照先が存在する
- タイトルの命名規則が統一されている

### フェーズ4：旧形式文書の構造移行

旧形式文書を分野または関連実装ごとの小さな単位で移行する。推奨順序は次のとおり。

1. 短い文書で移行方法を検証する
2. `dp/`、`string/`など比較的独立した分野を移行する
3. `graph/`、`math/`を移行する
4. 長いデータ構造系文書を移行する

各文書について次を行う。

- 概要文をfront matter直後へ配置する
- APIごとの説明をH1へ分割する
- `制約`と`計算量`を対応するAPIのH2へ配置する
- 複数APIを一つの箇条書きで説明している場合は、必要に応じてAPI単位に分割する
- 欠けている制約や計算量は、フォーマット変更とは別の内容修正として扱う

完了条件：

- API見出しと補助見出しの階層が標準形式に合っている
- 移行前後で説明の意味が変わっていない
- 公開ページで目次とコードブロックが正しく表示される

### フェーズ5：個別の不整合と内容不足の修正

機械的な統一後、文書ごとの問題を修正する。

初期対象には次を含める。

- `binary-indexed-tree.md`の`upper_bound`見出しレベル
- `binary-indexed-tree.md`の`prod`における計算量記載の確認
- `static-top-tree-dp.md`の本文構造
- APIシグネチャの言語指定漏れ
- 句読点、スペース、用語の不統一
- 実装と文書のAPI名、引数、戻り値の相違

内容を変更する場合は、必ず対応するhppを確認する。推測で制約や計算量を追加しない。

完了条件：

- 既知の個別不整合が解消されている
- 修正内容を実装と照合できる
- フォーマットだけの変更と内容変更をレビュー上で区別できる

### フェーズ6：CIでの強制

既存文書の移行が完了した時点で、CIのMarkdown検査を必須にする。

CIでは少なくとも次を検査する。

- markdownlintに違反していない
- Prettierの整形結果と差分がない
- front matterに`title`と`documentation_of`がある
- `documentation_of`の参照先が存在する
- コードブロックが閉じられている
- competitive-verifierによる文書生成が成功する
- Jekyllによるサイト生成が成功する

完了条件：

- 形式が崩れた文書を追加するとCIが失敗する
- テンプレートに従った文書はCIを通過する
- 既存の検証およびデプロイ処理に影響がない

## 5. 変更単位

レビューを容易にするため、変更は次の単位に分ける。

1. 設定、テンプレート、検査コマンド
2. 機械的な空白・リスト・コードフェンス修正
3. `title`追加
4. 分野ごとの旧形式移行
5. 実装確認を伴う内容修正
6. CIの必須化

一つの変更で全151文書を構造変換しない。特に長い文書は個別または少数単位で扱う。

## 6. 検証方法

各フェーズで次を確認する。

- Git差分に意図しない文章・数式の変更がない
- `documentation_of`の参照先が存在する
- コードフェンスの対応が崩れていない
- competitive-verifierによる文書生成が成功する
- Jekyllビルドが成功する
- 短い文書、長い文書、数式を含む文書、コード例の多い文書を代表として目視確認する

代表確認対象：

- `docs/dijkstra.md`：旧形式の短い文書
- `docs/union-find.md`：新形式の基本的な文書
- `docs/segment-tree.md`：コード例が多い長い文書
- `docs/binary-indexed-tree.md`：既知の見出し不整合がある文書
- `docs/static-top-tree-dp.md`：本文見出しがない文書

## 7. ロールバック方針

- 各フェーズを独立した変更として記録し、フェーズ単位で戻せるようにする
- 公開ページに問題が出た場合は、CIの必須化より先に文書変更を戻す
- 自動整形で広範囲な意図しない差分が発生した場合は、その設定を採用せず対象ルールを絞る
- URLやfront matterの解釈が変化する修正は、公開結果を確認するまで他の文書へ展開しない

## 8. 完了の定義

次をすべて満たした時点で移行完了とする。

- 151文書すべてが標準front matterを持つ
- 文書の見出し構造、リスト、コードフェンスが標準形式に従う
- 既知の個別不整合が解消されている
- Markdown検査がローカルとCIの両方で実行できる
- フォーマット違反をCIが検出する
- competitive-verifier/Jekyllによるサイト生成とデプロイが維持されている
- 新規文書の追加方法がテンプレートまたはREADMEで説明されている

## 9. 進捗

### フェーズ1

状態：実装完了（リモートCIでの実行確認待ち）

実施日：2026-07-30

実施内容：

- `templates/documentation.md`に新規文書用テンプレートを追加
- `.prettierrc.json`と`.prettierignore`を追加
- `.markdownlint-cli2.yaml`を追加
- `package.json`と`package-lock.json`でツールと実行コマンドを固定
- READMEにテンプレートとローカルコマンドの使用方法を追加
- CIにNode.js環境、依存関係のインストール、Markdown検査を追加
- テンプレートの検査は必須、既存文書全体の検査は移行完了まで非ブロッキングに設定

ローカル検証結果：

- テンプレート：Prettier、markdownlintともに成功
- npmの依存関係：再インストールに成功、既知の脆弱性なし
- 既存文書のPrettierベースライン：108ファイルに差分あり
- 既存文書のmarkdownlintベースライン：118ファイル、535件

既存文書全体に対する`npm run docs:check`は、フェーズ2以降の移行が完了するまで失敗する。CIではこの既知の失敗を結果表示に留めている。

### フェーズ2

状態：機械修正完了（リモートCIと公開ページの確認待ち）

実施日：2026-07-30

実施内容：

- 113文書へ意味を変えない機械的修正を適用
- 箇条書き357件を`*`から`-`へ変更
- C++コードフェンス99件へ`cpp`を指定
- 状態数一覧のコードフェンス1件へ`text`を指定
- 不要な行末空白31件を削除
- 表示上の改行に使われている行末2スペース15件を保持
- 全151文書のfront matter直後に空行があることを確認

ローカル検証結果：

- 変更前後の行数、見出し、本文、数式、コード内容が一致
- 箇条書き、フェンス言語、行末空白以外の差分がないことを正規化比較で確認
- 言語指定のないコードフェンス：0件
- `*`形式のMarkdownリスト：0件
- 不要な行末空白および空白だけの行：0件
- すべてのコードフェンスが閉じている
- すべての`documentation_of`参照先が存在する
- markdownlintのベースライン：42ファイル、51件
- Prettierのベースライン：44ファイル

残っているlint違反は、主に連続空行、見出しやコードフェンス周辺の空行、数式中の`*`の誤認識である。これらはフェーズ2で定義した変更範囲に含まれないため、このフェーズでは修正しない。

ローカル環境には生成済みの検証結果とJekyll実行環境がないため、competitive-verifier/Jekyllによる最終生成と代表ページの目視確認はリモートCIで行う。

### フェーズ3

状態：front matter統一完了（リモートCIと公開ページの確認待ち）

実施日：2026-07-30

実施内容：

- `title`がなかった61文書へ`title`を追加
- 公開ページの既存表示を維持するため、対応するhppの`@brief`をタイトルの基準として採用
- `Fibonacchi`となっていた2タイトルを`Fibonacci`へ修正
- 全151文書を対象にタイトル表記を確認し、85文書で英単語間のハイフンを空白へ変更、または日本語の丸括弧注記の前へ空白を追加
- `0-1`や`2-SAT`など、数値を含む標準的なハイフン表記は保持
- ファイル名、hpp名、`documentation_of`、本文およびAPI名は変更しない

ローカル検証結果：

- 151文書すべてに、空でない`title`と`documentation_of`が1件ずつ存在
- front matterをYAMLとして読み込めることを確認
- すべての`documentation_of`が重複せず、参照先が存在
- 追加した61タイトルは対応するhppの`@brief`を基にし、綴りとタイトル表記を上記ルールへ統一
- フェーズ3開始前との比較で、対象61文書の2行目への`title`追加以外に差分がないことを確認
- タイトル表記の統一前との比較で、対象85文書の`title`行以外に差分がないことを確認
- markdownlintのベースライン：42ファイル、51件（フェーズ2から増加なし）
- Prettierのベースライン：44ファイル（フェーズ2から増加なし）
- ルート文書とテンプレートはmarkdownlint、Prettierともに成功
- `git diff --check`に成功

URLはファイル名を変更していないため維持される想定だが、competitive-verifier/Jekyllによる最終生成、ページ一覧のタイトル表示、代表ページの目視確認はリモートCIまたは公開環境で行う。

### フェーズ4

状態：旧形式文書の構造移行完了（リモートCIと公開ページの確認待ち）

実施日：2026-07-30

実施内容：

- 移行開始時に本文の最初の見出しが `## 概要` だった79文書を移行
- 概要文をfront matter直後へ移動
- API名をH1、対応する制約と計算量をH2へ変更
- 複数APIを一括していた説明をAPI単位へ分割
- `cumulative-sum.md`と`cumulative-sum-2d.md`を含む全対象文書から、旧形式の`概要`と`使い方`見出しを解消

ローカル検証結果：

- 対象79文書の最初の本文見出しがすべてH1
- 対象79文書に `## 概要`、`## 使い方`、旧形式のAPI箇条書きが残っていない
- 制約と計算量が対象APIの配下に配置されている
- Prettier、markdownlint、文書構造検査に成功

### フェーズ5

状態：個別不整合の修正完了（リモートCIと公開ページの確認待ち）

実施日：2026-07-30

実施内容：

- 旧形式だった79文書の省略形API表記を、対応するhppの戻り値、引数、`const`を含むシグネチャへ変更
- クラスを提供する旧形式文書へ、実装で確認できるコンストラクタを追加
- `binary-indexed-tree.md`の`upper_bound`見出し、`prod`の計算量と境界条件を修正
- `static-top-tree-dp.md`へコンストラクタ、更新API、`TreeDPInfo`の要求インターフェースを記載
- `cumulative-sum.md`と`cumulative-sum-2d.md`へコンストラクタ、制約、API別計算量を記載
- `PalindromicTree`、Strongly Connected Components、Z Algorithmなどの未記載公開APIを補完
- `ConnectedGridStates`、各Dynamic Tree、Persistent Segment Tree、Fractional Cascading、オフライン矩形クエリ文書のシグネチャ不整合を修正
- Segment TreeとLazy Segment Treeの二分探索APIについて、戻り値型、未発見時の値、探索区間の説明を実装に合わせて修正
- コード、数式、URL、小数、Markdown番号を保持しつつ、日本語本文の句読点とその前後の空白を統一

ローカル検証結果：

- 旧形式だった79文書に省略形の関数シグネチャが残っていない
- 全151文書の識別子形式のAPI見出し472件を対応するhppと照合し、すべて実在
- 通常関数と演算子を含む戻り値型519件を対応するhppと照合し、すべて一致
- 言語指定のないコードフェンス：0件
- Prettier、markdownlint、文書構造検査に成功
- `git diff --check`に成功

competitive-verifier/Jekyllによる最終生成と公開ページの目視確認は、リモートCIまたは公開環境で行う。
