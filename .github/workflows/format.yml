name: clang-format

on:
  pull_request:

jobs:
  format:
    runs-on: ubuntu-24.04
    steps:
    - uses: actions/checkout@v3
      with:
        token: ${{ secrets.GITHUB_TOKEN }}
        ref: ${{ github.ref }}
    - name: install tools
      run: |
        sudo wget -qO - https://apt.llvm.org/llvm-snapshot.gpg.key | sudo apt-key add - 2>/dev/null
        sudo add-apt-repository "deb http://apt.llvm.org/`lsb_release -sc`/ llvm-toolchain-`lsb_release -sc`-22 main" 2>/dev/null
        sudo apt-get update
        sudo apt-get install clang-format-22
    - name: apply formatter
      run: |
        clang-format-22 -style=google --dry-run --Werror $(find . -name "*.hpp")
