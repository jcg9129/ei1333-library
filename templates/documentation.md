---
title: REPLACE_WITH_TITLE
documentation_of: //path/to/header.hpp
---

<!--
このファイルの使い方：

1. `docs/<ページ名>.md`へコピーします。
2. front matterの`title`と`documentation_of`を実際の値へ置き換えます。
3. 以下の説明文、型名、API名、引数などを実装に合わせて置き換えます。
4. 必須項目を記述し、該当しない任意項目は見出しごと削除します。APIが複数ある場合は「API名」の節を複製します。
5. `npm run docs:check`で形式を確認します。

記述ルール：

- ページ全体のタイトルはfront matterに書き、本文には書きません。
- タイトル内の英単語はハイフンではなく空白で区切ります。日本語の補足を丸括弧で併記する場合は、丸括弧の前に空白を1つ入れます（例：`Segment Tree (セグメント木)`）。
- コンストラクタ、関数、メンバー関数、参考文献など、ページ内の主要な節にはH1を使います。
- 引数、戻り値、制約、計算量など、主要な節の詳細にはH2を使います。
- C++のコードブロックには`cpp`を指定します。
- 概要、APIシグネチャ、動作、制約または前提条件、計算量は必ず記述します。
- テンプレート引数、記法、引数、戻り値、副作用、使用例、注意事項、参考文献は、該当する場合だけ残します。

このコメントは公開ページには表示されません。文書作成後に残しても削除しても構いません。
-->

ここに、データ構造またはアルゴリズムの目的、できること、利用場面を簡潔に記述します。

## テンプレート引数

<!-- 任意：テンプレートではない場合、この節を削除します。 -->

- `T`: 要素の型。必要な演算や満たすべき性質を記述します。

## 記法

<!-- 任意：文書内で使う変数や区間の規約が明らかな場合、この節を削除します。 -->

- $n$: 要素数
- 添字は0-indexedとし、区間は半開区間$[l, r)$で表します。

# コンストラクタ

<!-- コンストラクタがない場合は、この節全体を削除します。 -->

```cpp
TypeName(int n)
```

ここに、どのような状態で初期化されるかを記述します。

## 引数

- `n`: 要素数

## 制約

- $0 \leq n$

## 計算量

- $O(n)$

# API名

<!-- この節をAPIごとに複製し、「API名」を実際の関数名などへ置き換えます。 -->

```cpp
ReturnType operation(int l, int r) const
```

ここに、APIの動作と副作用を記述します。

## 引数

- `l`: 区間の左端
- `r`: 区間の右端

## 戻り値

ここに、戻り値の意味を記述します。結果が存在しない場合に返す値も明記します。

## 制約

- $0 \leq l \leq r \leq n$

## 前提条件

<!-- 任意：数値範囲以外の前提条件がない場合、この節を削除します。 -->

- ここに、ソート済み、連結、非負などの前提条件を記述します。

## 副作用

<!-- 任意：状態や引数を変更しない場合、この節を削除します。 -->

- ここに、更新される状態や破壊される引数を記述します。

## 計算量

<!-- 計算量で使う変数は「記法」またはこの節で定義します。 -->

- $O(\log n)$

## 使用例

<!-- 任意：基本的な呼び出し方が自明な場合、この節を削除します。 -->

```cpp
TypeName data_structure(n);
auto result = data_structure.operation(l, r);
```

## 注意事項

<!-- 任意：オーバーフロー、再利用可否、呼び出し順序などの注意がない場合、この節を削除します。 -->

- ここに、利用時に間違えやすい点や実装上の制限を記述します。

# 参考文献

<!-- 任意：参考文献や関連ページがない場合、この節を削除します。 -->

- [資料名](https://example.com/)
